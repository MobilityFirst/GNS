/**
 * The clientSupport package provides methods for NameServers to act like clients.
 * That is, NameServers need to read and write values from the GNS just like other clients.
 * This package provides the methods for that.
 */
package edu.umass.cs.gns.nsdesign.clientsupport;

