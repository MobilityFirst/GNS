language: java
jdk:
  - oraclejdk8

services:
  - mongodb
  
notifications:
  email:
    on_success: always # default: change
    on_failure: always # default: always
    
script:
  - ant testAlwaysFail

after_failure:
  - git revert "$TRAVIS_COMMIT"
  - git push

